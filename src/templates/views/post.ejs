<%- include('../partials/header.ejs') %>

<main class="container">
    <div class="row">
        <div class="col-md-6 offset-3">
            <div class="card mt-5">
                <div class="card-body">
                    <h1>Tweet</h1>
                    <p class="mb-0">Body</p>
                </div>
            </div>
            
            <form method="POST" action="/post/create">
                <div class="form-group">
                  <label for="title">Title</label>
                  <input type="text" name="title" class="form-control" id="title" aria-describedby="emailHelp" placeholder="Title of Tweet">
                </div>
                <div class="form-group">
                  <label for="description">Tweet</label>
                  <input type="text" name="description" class="form-control" id="description" placeholder="What's on your mind?">
                </div>
                <button type="submit" class="btn btn-primary">Submit</button>
            </form>
        </div>
        <div class="col-md-6 offset-3">
             <!-- <%# for (const post of user.posts) { %>
                <div class="card mt-4">
                    <div class="card-body">
                        <div class="form-group">
                            <label for="<%#= post.id %>">
                                <strong><%#= post.title %></strong>
                            </label>
                            <input type="text" name="<%#= post.id %>" class="form-control" required>
                        </div>
                    </div>
                </div>
            <%# } %> -->
        </div>
    </div>
</main>

<%- include('../partials/footer.ejs') %>